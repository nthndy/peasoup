# Local preview only. GitHub Pages builds the site for you on push, so you do
# not need to run this to publish. It is here so you can preview locally with
# `bundle exec jekyll serve` before pushing.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick"
